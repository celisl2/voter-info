module.exports = {
    plugins: [
        require('tailwindcss'),
        require('autoprefixer'),
    ]
}
/**
 * pecifies what PostCSS plug-in our project uses. In our case, it is Tailwind CSS and autoprefixer.
 * https://tailwindcss.com/docs/configuration
 */